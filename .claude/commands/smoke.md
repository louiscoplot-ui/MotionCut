---
description: Run an end-to-end smoke test of the generate pipeline
---

Spawn the `smoke-tester` sub-agent. Report its findings to the user in
under 100 words — pass/fail + the key numbers (job duration, output
size, any warnings).
